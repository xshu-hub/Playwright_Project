# ================================
# 使用说明
# ================================
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 根据需要修改参数值
# 3. .env 文件不会被提交到版本控制系统
# 4. 环境变量优先级: .env文件 > 系统环境变量 > 默认值
# 5. 布尔值请使用 true/false (小写)
# 6. 数字值请直接填写，不要加引号
# 7. 字符串值如包含特殊字符，请用引号包围

# 测试环境类型 (test/prod)
TEST_ENV=test

# 浏览器类型 (chromium/firefox/webkit)
BROWSER=chromium

# 无头模式 (true/false) - true为后台运行，false为显示浏览器窗口
HEADLESS=false

# 慢动作延迟 (毫秒) - 每个操作之间的延迟，便于调试观察
SLOW_MO=100

# 浏览器超时时间 (毫秒)
BROWSER_TIMEOUT=30000

# 页面导航超时时间 (毫秒)
NAVIGATION_TIMEOUT=60000

# 元素操作超时时间 (毫秒)
ELEMENT_TIMEOUT=10000

# 浏览器窗口宽度 (像素)
VIEWPORT_WIDTH=1920

# 浏览器窗口高度 (像素)
VIEWPORT_HEIGHT=1080

# 是否录制视频 (true/false)
RECORD_VIDEO=true

# 并行工作进程数 - 同时运行的测试进程数量
PARALLEL_WORKERS=1

# 最大重试次数 - 测试失败时的重试次数
MAX_RETRIES=2

# 重试延迟 (毫秒) - 重试之间的等待时间
RETRY_DELAY=1000

# 失败时是否重试 (true/false)
RETRY_ON_FAILURE=true

# 用户代理字符串 (可选) - 自定义浏览器标识
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# 语言设置
LOCALE=zh-CN

# 时区设置
TIMEZONE=Asia/Shanghai

# 日志级别 (DEBUG/INFO/WARNING/ERROR/CRITICAL)
LOG_LEVEL=INFO


